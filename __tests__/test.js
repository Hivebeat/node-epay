'use strict';

jest.dontMock('../')
    .dontMock('soap')
    .dontMock('q');

var NodeEpay = require('../');

describe('call', function () {
    it('throws error when no merhcantnumber set', function () {
        
    });
});
